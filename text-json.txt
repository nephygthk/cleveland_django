{
  "version": 2,
  "builds": [
    {
      "src": "clevhospital/wsgi.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "15mb",
        "runtime": "python3.9",
        "installCommand": "./vercel-build.sh"
      }
    }
  ],
  "routes": [
    { "src": "/static/(.*)", "dest": "/staticfiles_build/$1" },
    { "src": "/(.*)", "dest": "clevhospital/wsgi.py" }
  ]
}



this works with build.sh file